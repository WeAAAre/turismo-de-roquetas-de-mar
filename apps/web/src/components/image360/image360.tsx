'use client';
import React from 'react';
import Pannellum from 'pannellum-react/es/elements/Pannellum';

const Image360 = (props: React.ComponentProps<typeof Pannellum>) => {
  return <Pannellum {...props} />;
};

export default Image360;
